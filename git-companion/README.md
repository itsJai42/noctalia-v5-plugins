# Git Companion

Noctalia v5 Luau service, bar widget, and panel for GitHub or GitLab issue and PR/MR counts. Configure the platform and repository under **Settings → Plugins**; an authenticated `gh` or `glab` CLI and `jq` are required.
