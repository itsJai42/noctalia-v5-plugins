# Model Usage

Noctalia v5 Luau service, bar widget, and panel for local Claude Code or Codex session statistics. Select the provider under **Settings → Plugins**. The port reads local statistics only and requires `jq` for Claude data.
