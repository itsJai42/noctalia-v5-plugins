# Niri Auto Tile

Noctalia v5 Luau service and bar widget around the bundled `auto-tile.py` daemon. Configure startup, maximum visible columns, and resize behavior under **Settings → Plugins**; click the widget to start or stop it.
