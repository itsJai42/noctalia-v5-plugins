# Niri Overview Launcher

Noctalia v5 control-center shortcut that runs `niri msg action toggle-overview`.

The v4 keyboard-intercept overlay is intentionally absent because v5 plugins cannot create arbitrary layer-shell windows.
