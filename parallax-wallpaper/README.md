# Parallax Wallpaper

Deprecated for Noctalia v5. The v5 plugin API cannot create shell background surfaces or receive pointer position, so the QML parallax implementation has no native Luau equivalent yet.
